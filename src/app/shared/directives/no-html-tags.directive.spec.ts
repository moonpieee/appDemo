import { NoHtmlTagsDirective } from './no-html-tags.directive';

describe('NoHtmlTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new NoHtmlTagsDirective();
    expect(directive).toBeTruthy();
  });
});
